#!/bin/bash
SOURCE_FILE=$1
jq  -c '.result.transcriptList[] | { ts, username, text }' ${SOURCE_FILE} | sed 's#"username":#"name":#' | tee ${SOURCE_FILE//.json}.reduced.json
echo "reduced $1 to ${SOURCE_FILE//.json}.reduced.json"
